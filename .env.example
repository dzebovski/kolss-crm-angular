# Supabase (public keys only — safe for frontend)
# Angular sync script accepts SUPABASE_* or NEXT_PUBLIC_SUPABASE_* names.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Public app URL (links in notifications, redirects)
SITE_URL=http://localhost:4200
SITE_URL_PUBLIC=http://localhost:4200
