Read before editing anything in this repository:

@AGENTS.md
@best-practices.md

These are the canonical Angular/TypeScript conventions for this repo (also enforced for Cursor via `.cursor/rules/*.mdc` and mirrored for the Angular team's `angular-developer` skill). Treat `best-practices.md` as authoritative over general Angular knowledge when the two conflict — it encodes this project's Angular v22 choices (e.g. no explicit `standalone: true`, no explicit `OnPush`, `@Service` over `@Injectable({providedIn: 'root'})`, Signal Forms preferred).

For the full cross-repo KOLSS workflow (communication style, planning, verification, authorization boundaries), load the `how-to-dev-kolss` skill.
